{
  "name": "@danielklein/ci-sdk",
  "version": "0.0.3",
  "type": "module",
  "description": "Nx-native CI engine + CLI for Bun monorepos — codecheck / nx-sync / wrangler-types drift / typecheck / test, affected-aware. Bun-only.",
  "license": "MIT",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "bin": {
    "ci-sdk": "./dist/cli.js"
  },
  "files": ["dist", "src", "templates"],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && bun build ./src/index.ts ./src/cli.ts --outdir dist --target bun && tsc",
    "typecheck": "tsc --noEmit",
    "prepare": "bun run build"
  },
  "peerDependencies": {
    "nx": ">=20",
    "wrangler": ">=4"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.6.0"
  }
}
